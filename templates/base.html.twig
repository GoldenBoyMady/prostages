<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}ProStages{% endblock %}</title>

        {% block stylesheets %}
        {% endblock %}

    </head>

    <header>
        <h3><a>Filtrer par :</a></h3>
		<h3><a href="{{path("pro_stage_formations")}}">Formation</a></h3>
		<h3><a href="{{path("pro_stage_entreprises")}}">Entreprise</a></h3>

		<h1 align="center">ProStages</h1>

	</header>
    
    <body>
        {% block contenu %}
            {% for stage in stages %}
                <h1>{{ stage.titre }}</h1>
                <h1>{{ stage.mission }}</h1>
                <h1>-------------------------------------</h1>
            {% endfor %}
        {% endblock %}
    </body>
</html>
